version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/my-nestjs-app

hooks:
  AfterInstall:
    - location: scripts/install_and_build.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 60
      runas: root